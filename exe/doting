#!/usr/bin/env ruby

FILENAME = '.testrc'
FILEPATH = File.join(File.expand_path(ENV['HOME']), FILENAME)

case ARGV[0]
when 'install'
  File.open(FILEPATH, 'w') {}
when 'uninstall'
  File.delete(FILEPATH) if File.exists? FILEPATH
end
